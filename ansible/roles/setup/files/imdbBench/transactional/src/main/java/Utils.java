public class Utils {
    public static enum NewTitleOption {
        FromPreference,
        FromType,
        FromPopular
    }

    public static enum TransactionType {
        AddNewTitleToList,
        GetWatchListInformation,
        ViewTitle,
        RateTitle,
        SearchTitles
    }
}
